"use strict";

const { Store } = Vuex;

import { app, baseUrl } from "../config.js";

const store = new Store({
	state: () => ({
	}),
	mutations: {
	
	},
	actions: {
	},
	// modules: {},
});

export default store;
